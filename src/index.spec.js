import data from '../mocks'
import { getTweetsBySearchString } from './'

describe('getTweetsBySearchString', () => {
  it('returns an array of tweets whose text matches the provided search string', () => {

  })
})